# ─────────────────────────────────────────────────────────────
# SocialProof — Python Backend Dependencies  v3.0
# Tested against Python 3.11.9
# Install: pip install -r requirements.txt
# Then:    python -m spacy download en_core_web_sm
# ─────────────────────────────────────────────────────────────

# API Framework
fastapi==0.110.0
uvicorn[standard]==0.29.0

# NLP — spaCy (locked to avoid CLI crash)
spacy==3.7.4
typer==0.9.0
click==8.1.7

# Semantic Similarity
sentence-transformers==2.6.1

# NLI / Transformers
# roberta-large-mnli is the spec model
transformers==4.40.0
torch==2.2.2

# Database
sqlalchemy==2.0.29
pymysql==1.1.1

# Password hashing
bcrypt==4.1.3

# Utilities
pydantic==2.6.4
python-multipart==0.0.9
numpy==1.26.4
python-dotenv==1.0.1
email-validator

# Required for EmailStr in Pydantic
# (already covered by email-validator above)

# FAISS + BGE-M3 Corpus Retrieval
faiss-cpu==1.8.0
FlagEmbedding==1.2.5

# Corpus scraper + live search
requests==2.31.0
beautifulsoup4==4.12.3

# Async HTTP — v3 API integrations (Google Fact Check, ClaimBuster, MBFC)
httpx==0.27.0

# MBFC sync — iffy.news CSV parsing
pandas==2.2.2

# OCR — image input pipeline (pipeline/image_input.py)
# easyocr 1.7.2 pulls torchvision; install after base torch is confirmed working
easyocr==1.7.2

# PDF file submission support
pdfplumber==0.11.0

# Tagalog NLI translation fallback
deep-translator==1.11.4

# PostgreSQL driver (for Neon/Railway PostgreSQL)
psycopg2-binary==2.9.9
