[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "tesseract", "poppler_utils"]

[phases.install]
cmds = [
  "pip install --break-system-packages -r requirements.txt",
  "python -m spacy download en_core_web_sm"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
